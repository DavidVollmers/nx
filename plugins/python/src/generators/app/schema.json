{
  "$schema": "https://json-schema.org/schema",
  "$id": "DevTalesNxPythonApplication",
  "title": "Create a Python Application",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "A directory where the app is placed.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Which directory do you want to create the application in?"
    },
    "name": {
      "type": "string",
      "description": "Application name.",
      "x-priority": "important"
    },
    "framework": {
      "description": "Optionally set up a web framework for the application.",
      "type": "string",
      "enum": ["none", "fastapi"],
      "x-priority": "important"
    },
    "linter": {
      "description": "The tool to use for running lint checks.",
      "type": "string",
      "enum": ["none", "flake8"],
      "x-priority": "important"
    },
    "unitTestRunner": {
      "description": "Test runner to use for unit tests.",
      "type": "string",
      "enum": ["none", "pytest"],
      "x-priority": "important"
    },
    "publishable": {
      "type": "boolean",
      "default": false,
      "description": "Configure the application ready for use with `nx release` (https://nx.dev/core-features/manage-releases).",
      "x-priority": "important"
    }
  },
  "required": ["directory"]
}
