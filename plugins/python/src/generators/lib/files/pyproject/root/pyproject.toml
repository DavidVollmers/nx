[project]
name = "<%= name %>"
version = "0.0.0"<% if (classifiers.length) { %>
classifiers = [<%- classifiers %>]<% } %>
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = []

[tool.uv.sources]
